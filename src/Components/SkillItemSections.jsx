const SkillItemSections = ({ children }) => {
  return (
    <div className="flex items-center justify-center flex-wrap py-4 gap-4">
      {children}
    </div>
  );
};

export default SkillItemSections;
